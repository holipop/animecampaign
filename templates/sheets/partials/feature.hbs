<li class="feature" data-feature="{{_id}}">

    <!-- Entry -->
    <section class="entry" data-match-feature>

        <!-- Image -->
        <img class="img" src="{{img}}" title="{{localize @root.config.app.rollFeature}}" data-roll="{{_id}}" data-match-feature="background-color border-left-color">

        <!-- Name -->
        <a class="name" data-hide data-collapse-flag="feature.{{_id}}">{{name}}</a>

        <!-- Tracked Stats -->
        <div class="feature-tracked-stat-list">
            {{#each system.trackedStats as |stat index|}}

                <div class="stat">
                    <div class="stat-img">
                        <img src="{{stat.img}}">
                    </div>
                    
                    <div class="stat-numbers">

                        {{#if (eq stat.view 'value')}}
                            <span class="value">{{stat.value}}</span>
                        {{/if}}

                        {{#if (eq stat.view 'resource')}}
                            <span class="resource-value">{{stat.value}}</span>
                            <span class="resource-max">/&thinsp;{{stat.max}}</span>
                        {{/if}}

                        {{#if (eq stat.view 'label')}}
                            <span data-swap-font>{{stat.label}}</span>
                        {{/if}}
                        
                    </div>
                </div>

            {{/each}}
        </div>

        <!-- Controls -->
        <div class="feature-controls">

            <a class="control" title="{{localize @root.config.app.viewFeature}}">
                <i class="fas fa-feather-pointed" data-view-feature></i>
            </a>
            <a class="control" title="{{localize @root.config.app.deleteFeature}}">
                <i class="fas fa-trash" data-delete-feature></i>
            </a>

        </div>

    </section>

    <!-- Expanded View -->
    <ol class="expanded" data-match-feature="border-left-color" data-collapse-flag="target feature.{{_id}}">
        {{#each system.sections as |section index|}}
            <li>
                <a class="label" data-collapse-short="feature.{{../_id}}.{{index}}" {{#unless section.visible}}data-hide{{/unless}}>
                    <i class="fas fa-chevron-down"></i>
                    <span>{{section.name}}</span>
                </a>
                <div class="text" data-collapse-short="target feature.{{../_id}}.{{index}}">
                    {{{section.richtext}}}
                </div>
            </li>
        {{/each}}
    </ol>

</li>